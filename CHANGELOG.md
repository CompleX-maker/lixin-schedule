# 更新日志

本项目所有重要变更都会记录在此文件中。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [0.14.1] - 2026-09-24

### 修复
- **Server酱推送的 URL 构造有误**：原代码里三元表达式的两个分支完全相同（`key.startsWith("sctp") ? 老地址 : 老地址`），等于没做兼容
  - 后果：用户若填 Server酱³ 的 SendKey，会被官方「临时转发」接管，而该转发**会往消息里插入广告**，且官方声明最长三年后停用
  - 现在按 SendKey 格式区分两代产品，各自走官方入口：
    - `SCT...`（Turbo）→ `sctapi.ftqq.com/<key>.send`
    - `sctp{uid}t...`（³）→ `https://{uid}.push.ft07.com/send/<key>.send`
- **HTTP 200 不等于成功**：Server酱 失败时也返回 200，成功与否在 body 的 `code` 字段。原实现只看 `res.ok`，会把失败误判为成功
  - 改为解析 JSON 并检查 `code`，同时把官方错误翻译成人话（Key 无效 / 次数用尽 / 未关注）

### 新增
- **「测试推送」按钮**：配置后点一下就能立刻知道成没成功，无需等到上课提醒触发
- **SendKey 实时格式校验**：填错当场红字提示，并说明应为 `SCT` 开头的长串
- **Server酱配置图文引导**（可折叠）：
  - 三步说明，第一步直接给 sct.ftqq.com 跳转链接
  - 特别提示不要误填 Server酱³ 的 Key（`sctp` 开头，只能推到它自己的 APP）
- 新增接口 `testServerChan` / `testMail`

### 变更
- 「我的」页面的 SendKey 输入框替换为独立的 `ServerChanField` 组件，标题改为「微信推送（Server酱）」并加图标

### 数据
- 清理 `reminder_settings` 中一条无效数据（`serverChanKey = "server"`，测试残留）

## [0.14.0] - 2026-09-24

### 新增
- **PWA 支持：可「安装到桌面」，像 App 一样使用**
  - 解决用户反馈「每次都要打开微信或浏览器去翻网址」的问题
  - 手机上通过「添加到主屏幕」生成独立图标，点开全屏运行、无浏览器地址栏
  - 无需备案、无需签名、无需应用商店审核
  - 新增 `manifest.webmanifest`：`display: standalone`、主题色 `#d9e84a`、4 个图标（含 maskable）
  - 新增 Service Worker（`/sw.js`）：
    - 站点可安装的前提条件之一
    - 静态资源（`/assets/`、`/static/`）缓存优先，打开更快
    - 页面导航网络优先、离线回退缓存，断网时可打开上次页面
    - API 请求完全不缓存，保证课表数据实时
  - **安装引导（悬浮按钮 `InstallFab`）**：首页右下角常驻，覆盖所有环境
    - Chromium 系（Chrome / Edge）：捕获 `beforeinstallprompt`，点击直接唤起系统安装弹窗
    - 安卓非 Chromium 浏览器（Via / Firefox / UC）：展开「右上角 ⋮ → 添加到主屏幕」手动步骤
    - iOS Safari：展开「分享 → 添加到主屏幕」分步图文
    - 桌面浏览器：提示地址栏安装图标位置
    - 已安装或用户忽略后 14 天内不再显示
  - **微信内置浏览器引导（`WeChatGuide`）**：访问占比约一半，而微信 WebView 完全不支持安装
    - 进入页面 2.5 秒后从顶部滑下轻提示条，引导「右上角 ⋯ → 在浏览器打开」
    - 刻意不用全屏遮罩，避免打断阅读；可关闭，12 小时内不重复
  - 图标由站内 Q 版形象（八千代）生成，配站点主色底（直接用透明底会导致图标几乎不可见）
  - 新增 manifest `shortcuts`：长按图标可直达「今日 / 本周 / 广场」
  - iOS 支持：`apple-mobile-web-app-capable`、`apple-touch-icon`、状态栏样式

### 变更
- `index.html` 补充完整 meta（description、theme-color、viewport-fit=cover 适配刘海屏）
- Nginx 增加 PWA 相关规则：`/sw.js` 走 `no-cache` 并带 `Service-Worker-Allowed`；manifest 指定 `application/manifest+json`；图标目录 30 天缓存
- 移除冗余的 `InstallBanner`（「我的」页内联条），由全局悬浮按钮 `InstallFab` 取代，避免同页重复提示

### 修复
- **安卓非 Chromium 浏览器看不到安装入口**：原逻辑为「有 `beforeinstallprompt` → 显示按钮 / 是 iOS → 显示步骤 / 否则不显示」，而 Via、Firefox、UC 等不派发该事件，导致按钮完全不渲染
  - 改为「只要未安装就一定有引导」：按平台分别给出原生安装或手动添加步骤

### 说明
- PWA 与「微信小程序」「原生 App」的区别：PWA 免备案免签名，适合课程表这类工具
- 已知限制：iOS 上必须用 Safari 才能添加；微信内置浏览器无法安装（已做引导）
- **微信推送的可行性**：个人主体的订阅号无法完成微信认证，因而拿不到模板消息权限；唯一可能的路径是「一次性订阅消息」（用户每授权一次可发一条，无需关注），但同样要求账号**已认证**，需在公众平台「接口权限」中确认后另作评估

## [0.13.0] - 2026-09-23

### 新增

- **留言回复**：留言墙支持楼中楼回复
  - 每条留言可回复；回复也可以再被回复，自动带上「回复 @某人」标记
  - 回复列表可折叠 / 展开，回复数直接显示在留言下方
  - 回复同样限流：30 秒 1 条
  - 删除权限：本人或管理员
- **代课悬赏（广场新板块）**：与留言墙分栏划分，互不混排
  - 发布悬赏：课程名称、上课地点、星期、起止节次、具体日期（可选）、价格（或勾选「价格面议」）、补充说明 **全部自定义**
  - **接单流程**：可报名接单 → 发布者从报名者中挑选确认 → 状态流转（招募中 / 已接单 / 已完成 / 已取消）
  - 接单者可取消报名（已被选中的不能自行取消）；不能接自己发的单
  - 报名名单仅发布者与管理员可见
  - 发布限流：2 分钟 1 条
  - 页面底部注明「仅提供信息发布，不参与交易与担保」
- **管理员可见真实身份**：留言、回复、悬赏发布者与接单者旁边，管理员可见红色徽标显示真实**姓名 + 学号**；普通用户与未登录用户只能看到昵称
  - 身份在服务端按 `ctx.user.role === "admin"` 判定后才下发，非管理员拿到的 `identity` 恒为 `null`
  - 不采集 IP，仅用站内已有的学号姓名做追溯
- **按教学楼的精确作息表**：解决「同一校区内不同教学楼上下课时间不同」的问题
  - 依据学校《上课时间表》整理出 **4 套作息组**：
    | 作息组 | 教学楼 | 第 3 节 | 第 4 节 |
    |---|---|---|---|
    | 松江 A | 学验楼、育才楼、体育馆 | 10:20 | 11:10 |
    | 松江 B | 吉祥楼、北碚楼、徐汇楼、云间楼 | 10:30 | 11:20 |
    | 浦东 A | 六教、体育馆 | 10:20 | 11:10 |
    | 浦东 B | 二教、五教、实验中心 | 10:30 | 11:20 |
  - 自动识别教室名：既支持中文楼名（`六教202`、`实验中心702`），也支持松江编码（`3DM52`、`4DM42`、`2D121`、`3S12`）
  - 经真实课程数据验证：260 个不同教室中 **258 个可自动归类**（覆盖率 99.2%），剩余 2 个为「大学城体育中心」「高尔夫球场」等体育场馆，按松江 A 组兜底
  - 新增接口 `periodGroups`（返回全部作息表）与 `resolveLocation`（由教室名解析校区与时刻）
  - 代课悬赏的具体时间由**服务端**按所选教室换算，不信任前端传值

### 修复
- **代课悬赏只能发布、无法接单**：`subApply` 里用 `listSubstitutePosts(userId, 1, "all")` 做重复报名检查，但 `limit=1` 只会返回最新一条，`filter(p => p.id === postId)` 几乎永远拿不到数据 —— 重复检查形同虚设，同时「我接单」按钮藏得太深
  - 服务端改为新增 `getMyApplication(postId, userId)` 直查，去掉 `limit` 依赖
  - `subDetail` 补返回 `myApplyStatus`，前端可正确判断「已报名 / 待挑选 / 已选中」
- **接单入口不明显**：原来仅当 `status === "open" && !isMine` 时显示，且只是一个小文字链
  - 改为带边框的显式按钮；未登录显示「登录后可接单」；自己发的单不显示
  - 报名为空时，详情展开区也提供「我接单」入口
  - 已报名后显示「已报名（点击取消）」并可取消
- **默认筛选是「招募中」**：刚发布就被筛掉、已接单的也看不到，改为默认「全部」，并把「全部」放到筛选第一位

### 变更
- 广场改为板块切换布局：顶部「戳一戳」，下方「留言墙 / 代课悬赏」两个独立板块
- 留言时间改为相对时间显示（刚刚 / N 分钟前 / N 小时前 / N 天前），悬停显示完整时间

### 数据库
- 新增 `wall_replies` 表（留言回复，含 `replyToId` / `replyToNickname`）
- 新增 `substitute_posts` 表（悬赏单，含节次 + 换算后的具体时刻 + 状态机）
- 新增 `substitute_applications` 表（报名记录，`uniq_sub_apply` 保证同一人对同一单只能报一次）
- 新增接口：`wallReply` / `wallReplyDelete` / `periodGroups` / `resolveLocation` / `subList` / `subDetail` / `subCreate` / `subApply` / `subCancelApply` / `subAccept` / `subUpdateStatus` / `subDelete`

## [0.12.0] - 2026-09-22

### 新增
- **广场（新标签页）**：底部导航新增「广场」，集中放置互动内容
  - **戳一戳**（置顶）：Q 版形象（八千代），点一下换一句立信相关文段；文案池复用段子库并补充互动口吻，共 50+ 条
  - **留言墙**：公开的需求 / 建议 / 吐槽墙，帮助了解大家真正想要什么
    - 可自定义昵称（默认填真实姓名，可清空改写）
    - 分类标签：需求 / 建议 / 吐槽 / 其他
    - 点赞（同一人限一次，可取消）、删除（本人或管理员）
    - 筛选：全部 / 我的；发布限流：60 秒 1 条
- **登录页同步加入「戳一戳」**：未登录用户也能看到 Q 版形象与文段，登录失败时气泡会显示错误提示
- 新增 `wall_messages`、`wall_likes` 数据表与 4 个接口（`wallList` / `wallPost` / `wallToggleLike` / `wallDelete`）

### 变更
- 留言墙从「我的」页面移至「广场」，与戳一戳合并为互动区

### 修复
- **留言昵称删不掉**：昵称输入框用 `form.nickname || defaultNickname` 回填，导致清空后又被默认值填回。改为「已修改」标记 + 空值合并，现在可以彻底删空

### 优化（服务端稳定性与风控）
- **关闭定时全量同步**（`ENABLE_AUTO_SYNC` 默认 false）
  - 原因：绑定账号增至 200+ 后，每天 2 次全量同步会在同一分钟内产生上百次 CAS 登录（实测 07:00 曾集中 108 次），具备明显爬虫特征，存在被教务系统风控识别甚至封禁账号的风险
  - 现在：定时任务只做上课提醒与次日课表推送（纯本地数据库查询，不访问教务系统），课表更新改为用户手动触发
  - 如需恢复：设置环境变量 `ENABLE_AUTO_SYNC=true`
- **手动同步增加 5 分钟冷却**：避免频繁请求教务系统
- **登录/同步失败提示优化**：账号密码类错误保留原文（便于用户自行修正），教务系统不可达等情况统一显示友好文案

### 新增（管理员）
- **访问统计**（详见 0.11.0）：累计访问人数/次数、今日/近 7 天/近 30 天、访问名单、最近流水；不记录 IP

## [0.11.0] - 2026-09-20

### 新增
- **访问统计（仅管理员可见）**：「我的 → 访问统计」面板，帮助了解工具的实际使用情况
  - 总体数据：累计访问人数（按账号去重）、累计访问次数
  - 时间维度：今日 / 近 7 天 / 近 30 天各自的人数与次数
  - 访问名单：真实姓名、学号、学院、首次访问时间、累计次数、最近访问时间，按最近访问倒序
  - 最近流水：按时间倒序的访问明细（默认 60 条）
  - 面板每分钟自动刷新，也可手动刷新
- 新增 `visit_logs` 数据表与 `schedule.visitStats` 接口（`adminQuery` 保护，非管理员访问返回 403）

### 隐私说明
- 访问记录**不保存 IP 地址与设备/浏览器信息**，仅保存学号、姓名、学院快照与访问时间
- 同一账号 **5 分钟内重复访问只记一次**（避免刷新页面刷屏）
- 统计写入失败不会影响正常登录与课表同步（异常被吞掉，不阻断主流程）
- 学院信息依赖课表同步时从教务系统提取，未同步过的账号可能为空

## [0.10.0] - 2026-09-20

### ⚠️ 重大变更
- **学校已为教务系统接入深信服 aTrust 零信任网关**，`lxjw.lixin.edu.cn`、`courses.lixin.edu.cn` 等教务相关域名全部置于网关之后。此前"纯 CAS + 直连抓取"的链路（见 README）已**无法在服务器上直接工作**：带 TGC 访问教务系统会被 302 重定向到 `atrust.lixin.edu.cn` 的 JS 环境校验页，纯 `fetch` 无法通过。
  - 症状：登录时提示「登录失败，检查学号密码，或稍后再试」；后端日志/db `fetch_logs` 无成功记录
  - 注意：**账号密码是正确的**，CAS 认证本身可以成功（能拿到 `TGC`），失败发生在「用 TGC 换取教务会话」这一步

### 新增
- **自部署 aTrust 客户端方案**（服务器侧恢复课表同步的完整路径，已在 Alibaba Cloud Linux 3 实测通过）：
  1. 从网关自带的下载地址获取 Linux 客户端（无需自行寻找安装包）：
     - `https://<网关域名>/resource/client/linux/ubuntu/amd64/aTrustInstaller_amd64.deb`（x86_64）
     - 该 `.deb` 本质是 `ar` 归档，可用 `ar x` 解包后直接取 `data/usr/share/sangfor/aTrust`
  2. 补依赖（RHEL 系）：`gtk3 libXcomposite libXcursor libXdamage libXi libXrender libXtst atk at-spi2-atk gdk-pixbuf2 pango cairo libXrandr mesa-libgbm libXScrnSaver alsa-lib at-spi2-core libproxy libX11 mesa-libGL`
  3. 核心服务：`aTrustAgent -i` 注册服务后 `systemctl enable --now aTrustDaemon`
  4. 图形环境：装 `xfce4-session` + `tigervnc-server`（或 Xvfb），让客户端有可用桌面
  5. **关键**：`aTrustCore` 插件必须带 `--enable-http` 启动，否则插件内部 `setHttpReferer` 断言失败并 `SIGABRT` 退出，客户端会一直提示「核心服务未启动」：
     ```bash
     aTrustAgent --plugin plugins/aTrustCore --plugin-cmd "|" --enable-http --enable-event-center
     ```
     成功标志：`/usr/share/sangfor/.aTrust/var/run/aTrustCore-<uuid>` socket 被创建，且与客户端期望的 UUID 一致
  6. 客户端托盘需 `--no-sandbox --disable-gpu --disable-gpu-sandbox` 才能在无 GPU 的无头环境稳定运行（缺 `--disable-gpu-sandbox` 会因 `GPU process isn't usable` 反复崩溃）
  7. 服务器需安装浏览器（`firefox`）与 `xdg-utils`，否则登录页「前往浏览器登录」无法拉起
  8. 登录方式推荐**企业微信扫码**（无需在服务器上处理 CAS 表单），登录成功即代表服务器"上线"，此后服务端 `fetch` 访问教务系统自动走隧道

### 修复
- `ADMIN_STUDENT_IDS` 未生效：README 文档与部分 `.env` 中误写为单数 `ADMIN_STUDENT_ID`，而代码读取的是复数形式，导致管理员名单不起作用。现已改正文档；若你的 `.env` 用了单数形式，请改为 `ADMIN_STUDENT_IDS=<学号1>,<学号2>`

### 说明
- aTrust 客户端登录后会话有有效期，掉线需重新登录（VNC 连上去扫一次码即可）。建议配合 systemd 做守护：`aTrustDaemon`、`aTrustCore` 插件、客户端托盘分别为独立服务并设 `Restart=always`

## [0.9.0] - 2026-09-19

### 新增
- 课程总览：课表页面向左翻到第 1 周之前即是总览视图，按课程分组列出全部上课安排（星期节次、起止时间、教室、周次、单双周、备注标记），点任意一条可查看详情和写备注
- 全站通知（调课提醒）：管理员在「我的 → 管理员配置」发布通知后，所有用户打开网页即弹窗展示；支持 1 天 / 3 天 / 7 天后自动关闭或手动关闭，同一时间只生效一条，用户点「我知道了」后同一条不再打扰
- 页脚署名「Designed by 初晓」

## [0.8.2] - 2026-09-19

### 修复
- 第 1 周周四/周五的课不显示：教务系统 weekstate 周次掩码需要按 newActivity 的 startOn 参数做按星期几的位移（周四/五课程与周一至三课程锚点不同），此前解析器忽略该参数导致周四/五课程整体后移一周。现已完全对齐教务官方 TaskActivity.js 的 weeksBetween + convertWeekstate2ReverseString 算法，并用 25 届、26 届两个账号与教务官网逐门课核对一致（概率论、大学英语、专项体育等第 1 周课程恢复显示；数据库原理、计算机网络基础等单双周归属同步纠正；职业规划等课由错误的 7-10 周更正为 6-9 周）
- 已为全部 116 个已绑定账号用修复后的解析器重新同步课表，无需用户重新登录
- 层级问题：课程块滚动时不再盖住顶部栏和底部导航（顶栏/底栏 z-index 提升）

## [0.8.1] - 2026-09-18

### 修复
- 26 届新生课表对不上的问题：学期日历（开学日期、节次时间）不再使用全局统一配置，改为每个账号同步时从教务系统单独提取、单独保存，不同年级/校区按各自实际日历显示周次和上课时间

## [0.8.0] - 2026-09-18

### 新增
- 课表网格改为按每一小节一行（13 行），每行标注节次和起止时间
- 课程块显示起止时间（如 08:30-11:05）；仅单周/双周上的课显示「单」「双」角标
- 课程详情弹窗的时间一栏补充具体钟点

### 修复
- 课表内容错位：3 节连上或单节的课程现在按实际节数跨行占位，不再被两节一大行压缩
- 单双周教室混淆：同一门课单周和双周在不同教室上课时拆成两条记录，课表随周切换只显示当周的教室

## [0.7.0] - 2026-09-18

### 新增
- 课程备注：课表页点开课程块即可记录备注（作业 DDL、考试范围等），按「课程名+星期+节次」稳定标识独立存储，课表重新同步后备注不丢失；有备注的课程块右上角显示便签图标
- 段子播报改为悬浮药丸样式：圆角胶囊 + 描边 + 淡阴影，与底部导航保持间距，字号加大

### 变更
- 「清除数据」现在会一并清除课程备注

## [0.6.0] - 2026-09-18

### 新增
- 立信段子轮播条：底部导航上方每 5 秒自动切换一条（辟谣自嘲 / 校史冷知识 / 教务吐槽 / 校园生活 / 会计梗五类共 22 条），上滑淡入动画
- 底部导航动画：活动标签背后橄榄色指示 pill 弹簧滑动，图标选中放大、按压回弹
- 课表切换周数动画：网格整体按方向滑入滑出，周数标题同步过渡
- 三个标签页之间的切换转场、登录页入场动画

## [0.5.0] - 2026-09-18

### 新增
- 自部署支持：README 新增完整部署章节（环境变量模板、构建与启动步骤）
- `ADMIN_STUDENT_IDS` 环境变量：名单内学号登录后自动成为管理员，解决自部署时没有 Kimi 后台、无法设置管理员的问题

## [0.4.0] - 2026-09-18

### 新增
- 页脚 GitHub 开源入口（求 Star）
- README：项目介绍、技术栈、CAS 登录链路逆向文档、MIT 许可证、免责声明

## [0.3.1] - 2026-09-18

### 修复
- 未登录用户误入空课表页的问题：现在只有教务身份（学号登录）才能直接进入 App，Kimi 管理员需从登录页「管理员入口」显式进入

## [0.3.0] - 2026-09-18

### 新增
- 学生直接使用学号 + 统一身份认证密码登录，不再需要 Kimi 账号绑定
- 登录加载进度条（先快后慢、等待接口补满）+ 轮播小玩笑
- 每个人的课表独立保存，新设备需重新登录（会话 30 天有效）

### 变更
- 密码改用 AES-256-GCM 加密存储，仅用于课表同步

## [0.2.0] - 2026-09-18

### 新增
- 教务系统抓取链路实测打通：CAS 登录（RSA 加密密码 → TGC 票据）→ Beangle EAMS 课表接口 → weekstate 位图解析（53 位 ISO 周年掩码，支持单双周、跨年周）
- 学期自动校准：每次同步从课表页面提取学期开始日期与节次时间表，无需手工配置
- 同一天同一节次的单双周课程自动合并、多教室合并显示

## [0.1.0] - 2026-09-18

### 新增
- 首个可用版本：课表同步 + 上下课倒计时 + 提醒
- 「今日」页：环形倒计时（上课中 / 下一节课 / 没课三态）、当天课程时间线
- 「课表」页：整周网格视图、周切换、课程详情弹窗
- 「我的」页：账号状态、手动同步、提醒设置（邮件 / Server酱）、同步日志
- 后台调度：每日早晚两次自动同步课表、上课前提醒、每晚推送次日课表
- 管理面板：学期配置、SMTP 发信配置
